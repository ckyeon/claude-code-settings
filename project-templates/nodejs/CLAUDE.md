# {{PROJECT_NAME}}

Node.js project. Replace `{{PROJECT_NAME}}` and fill in the sections below.

## Architecture

(Brief overview.)

## Commands

- Install: `npm install`
- Test: `npm test`
- Lint: `npm run lint`
- Build: `npm run build`

## Conventions

- Package manager: npm (swap to pnpm/yarn if applicable)
- Node version: pinned via `.nvmrc` or `package.json` engines
