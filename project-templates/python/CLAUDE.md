# {{PROJECT_NAME}}

Python project. Replace `{{PROJECT_NAME}}` and fill in the sections below.

## Architecture

(Brief overview.)

## Commands

- Install: `pip install -r requirements.txt` or `uv sync`
- Test: `pytest`
- Lint: `ruff check .`
- Format: `ruff format .`

## Conventions

- Python version: pinned in `pyproject.toml` or `.python-version`
- Package manager: pip / uv / poetry (specify)
