# 0038 — 2026-09-02 일괄 재핀 (13건) + github README scope 문구 갱신

ADR 0037의 구조 스캔이 포함된 `/check-updates` 첫 실전 실행 결과를 반영했다. outdated 13건을 전부 upstream HEAD로 재핀: claude-plugins-official 5건(→ 0e3f501d), github-mcp-server(→ 12d16ed0), context7(→ c05c71c5), modelcontextprotocol/servers 2건(→ 2e3e4c7a), vercel/next.js 스킬 4건(→ 483f8420, copied이지만 pin-only라 파일 내용 변화 없음). 구조 변화(신규/삭제 스킬)는 0건. 실질 변경은 github-mcp-server 한 곳뿐: upstream `198bc16`이 `public_repo`만 있는 classic PAT에도 public-repo write tool 일부를 노출하도록 바꿔서, README의 "`repo` 없이는 모든 write tool 숨김" 문구를 현행에 맞게 완화했다. 등록 명령·권장 scope 조합은 모두 그대로 유효함을 판단 agent가 확인했다.
