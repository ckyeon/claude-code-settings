# 0031 — 2026-09-01 upstream update sweep

First full run of the reworked check-updates pipeline (ADR 0029): `--deep` classified 62 outdated items into 23 changed / 39 pin-only, and five judgment subagents reviewed every changed item. Decision: update everything. Changed `copied` items are re-adopted for content (mattpocock/skills ×12 — Redact section in diagnosing-bugs, cross-skill calls rewritten to the quoted Skill-tool phrasing covered by ADR 0030; vercel/next.js ×4 as one interdependent set; react-view-transitions, which retracts the deprecated `experimental.viewTransition` flag; agent-browser core). Changed `inspired-by` items (github, context7, fetch, sequential-thinking, understand-anything) get pin refreshes only — all locally documented registration commands were verified still valid, with context7's README gaining the newly advertised `X-Context7-API-Key` canonical header. The 39 pin-only items (phaser ×28, official plugins ×5, agent-skills ×5, dogfood) are re-pinned for accuracy; no local drift existed anywhere, so no local edits were lost.
